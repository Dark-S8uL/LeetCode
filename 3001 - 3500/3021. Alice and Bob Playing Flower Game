class Solution {
public:
    long long flowerGame(int n, int m) {
        long long cnt = 0 ;
        long long a = n/2, b = (n+1)/2 ;
        long long c = m/2, d = (m+1)/2 ;
        cnt += (a * d) + (b * c) ;
        return cnt ;
    }
};
