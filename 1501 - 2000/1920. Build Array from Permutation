class Solution {
public:
    vector<int> buildArray(vector<int>& nums) {
        int n = nums.size() ;
        if(n == 1) return nums ;
        vector<int> v(n) ;
        for(int i = 0; i < n; i++) {
            v[i] = nums[nums[i]] ;
        }
        return v ;
    }
};
