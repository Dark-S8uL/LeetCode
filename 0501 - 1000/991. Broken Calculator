class Solution {
public:
    int brokenCalc(int st, int t) {
        int cnt = 0 ;
        while(t != st) {
            if(t % 2 == 0 && t > st) t /= 2 ;
            else t++ ;
            cnt++ ;
        }
        return cnt ;
    }
};
