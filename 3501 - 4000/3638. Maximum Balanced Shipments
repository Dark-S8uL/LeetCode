class Solution {
public:
    int maxBalancedShipments(vector<int>& wt) {
        int n = wt.size() ;
        int cnt = 0 ;
        for(int i = 1; i < n; i++) {
            if(wt[i] < wt[i-1]) {
                cnt++ ;
                i++ ;
            }
        }
        return cnt ;
    }
};
